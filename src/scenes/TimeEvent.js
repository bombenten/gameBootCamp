import Phaser from "phaser";


class TimeEvent extends Phaser.Scene {
    constructor(test) {
        super({
            key: 'TimeEvent'
        });
    }

    preload() {

       }

    create() {
            
    }

    update(delta, time) {
        
    }
}

export default TimeEvent;
